export interface IButtonRedProps {
	textBtn?: string;
	children?: React.ReactNode;
	segmentEvent: string;
	titleHover: string;
	type: 'submit' | 'reset' | 'button';
	typeElement: string;
	classes: string;
};
